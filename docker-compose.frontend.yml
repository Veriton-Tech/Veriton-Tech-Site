services:
  atveriton-frontend:
    build: .
    image: atveriton-frontend:latest
    restart: always
    container_name: atveriton-frontend
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - proxy

networks:
  proxy:
    external: true